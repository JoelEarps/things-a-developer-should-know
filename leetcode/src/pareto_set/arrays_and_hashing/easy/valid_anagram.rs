/*
242. Valid Anagram
Easy
Topics
premium lock icon
Companies
Given two strings s and t, return true if t is an anagram of s, and false otherwise.

 

Example 1:

Input: s = "anagram", t = "nagaram"

Output: true

Example 2:

Input: s = "rat", t = "car"

Output: false

Constraints:

1 <= s.length, t.length <= 5 * 104
s and t consist of lowercase English letters.
 

Follow up: What if the inputs contain Unicode characters? How would you adapt your solution to such a case?

P - we want to check if two strings have exactly the same characters and frequency
E - s = "anagram", t = "nagaram" - true
   s = "rat", t = "car" - false
A - strings are not the same length, either or both string lengths are zero

P
I
E

*/

pub fn is_anagram(_s: String, _t: String) -> bool {
    unimplemented!("Boilerplate only; implement an anagram check")
}

